const formulario=document.querySelector('#formulario');formulario.addEventListener('submit',evento=>{evento.preventDefault();const base=Number(document.querySelector('#servicio').value);const cantidad=Math.max(1,Number(document.querySelector('#cantidad').value));const premium=document.querySelector('#soporte').checked;const subtotal=base*cantidad;const total=premium?subtotal*1.2:subtotal;document.querySelector('#resultado').textContent=`Total estimado: $${total.toFixed(2)}`});
