# UNE4D27 — Ejercicio guiado de métodos push, pop y splice

Práctica guiada del Nivel 4. Modifica un arreglo desde una interfaz y observa el resultado de `push()`, `pop()` y `splice()`.

Abre `index.html` en el navegador.
