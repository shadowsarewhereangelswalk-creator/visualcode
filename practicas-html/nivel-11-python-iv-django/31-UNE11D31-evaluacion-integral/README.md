# UNE11D31 — Evaluación integral, corrección de errores y cierre del Nivel 11

Clase 31 del Nivel 11 — Python IV: Django.

- **Práctica:** Evaluación integral, corrección de errores y cierre del Nivel 11
- **Resultado terminado:** Gestor de incidencias con formulario, panel, API y pruebas integrales.
- **Herramientas:** Python, Django, formularios, JSON y pruebas y Visual Studio Code
- **Proyecto del nivel:** Aplicación Django

1. Crea y activa un entorno virtual.
2. Ejecuta `python -m pip install -r requirements.txt`.
3. Ejecuta `python manage.py migrate`.
4. Ejecuta `python manage.py runserver`.

Ejecuta `python manage.py test core` para comprobar la práctica automatizada.

Comprueba la estructura con `python manage.py --check` y el proyecto con `python manage.py check`.

