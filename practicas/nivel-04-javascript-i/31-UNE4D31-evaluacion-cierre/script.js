const pruebas=[];const evaluar=(nombre,condicion)=>pruebas.push({nombre,correcta:Boolean(condicion)});const sumar=(a,b)=>a+b;evaluar('función con retorno',sumar(2,3)===5);const datos=[1,2];datos.push(3);evaluar('push',datos.length===3);datos.splice(1,1);evaluar('splice',datos.join(',')==='1,3');const nodo=document.createElement('strong');nodo.textContent='DOM';evaluar('creación DOM',nodo.textContent==='DOM');const lista=document.querySelector('#resultados');pruebas.forEach(p=>{const li=document.createElement('li');li.className=p.correcta?'ok':'error';li.textContent=`${p.correcta?'✓':'✗'} ${p.nombre}`;lista.append(li)});
