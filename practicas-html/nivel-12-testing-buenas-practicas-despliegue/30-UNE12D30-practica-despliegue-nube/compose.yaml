services:
  web:
    build: .
    ports:
      - "10000:10000"
    environment:
      APP_NAME: Deployment Control
      APP_ENV: production
      APP_VERSION: 1.0.0
      RELEASE_SHA: compose
      PORT: "10000"
    restart: unless-stopped

